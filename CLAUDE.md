# overseers-desk — notes for AI sessions

The Claude Code plugin marketplace for the Overseer's Desk: `.claude-plugin/marketplace.json` lists the plugins, the README is the homepage that introduces the whole tool estate.

The mirroring rule is in [`INVARIANTS.md`](INVARIANTS.md); a change that breaks it is a design change, the owner's to make.
